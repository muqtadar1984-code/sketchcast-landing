# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# local editor / preview tooling
.claude/

# the site has no dependencies; this is only ever npx's scratch space
node_modules/
